# Canonical origin used for Open Graph, JSON-LD, sitemap, and robots.txt.
# No trailing slash. Set this on Vercel for Production and Preview.
PUBLIC_SITE_URL=https://example.com

# Optional. Formspree-style endpoint for email capture + contact.
# Leave empty to use the local placeholder / mailto fallback (no database).
PUBLIC_FORM_ACTION=
